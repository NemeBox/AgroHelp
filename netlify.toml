# Netlify Build & Deploy Settings

[build]
  command = "npm install" # Installs backend dependencies
  publish = "."           # Publishes the root directory containing your HTML files
  functions = "functions" # Specifies the directory for serverless functions

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200